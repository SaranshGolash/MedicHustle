# App
PORT=3000
SESSION_SECRET=super-secret-session-key-change-me

# PostgreSQL (local dev example)
DB_USER=postgres
DB_PASSWORD=Saransh@2003
DB_HOST=localhost
DB_PORT=5432
DB_NAME=civicease_db

# Combined URL (used by app.js)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
